<article index="true" numberBeforeTitle="true">
    <topic>
        <title>Using Terminal UI</title>
        <p>
            <name>vl::console::TUI</name> provides terminal input, cell drawing and screen updates on Windows, Linux and macOS.
            Its public types are declared in <name>Source/TUI/TUI.h</name> and shipped in <name>VlppOS.h</name>.
            For an application built from GacUI controls, use the <a href="/gacui/tui.html">GacUI terminal setup functions</a>, which manage this lower-level session for you.
        </p>
        <topic>
            <title>Session and callbacks</title>
            <p>
                Implement <name>ITuiCallback</name>, install the listener with <name>TUI::InstallListener</name>, and call <name>TUI::Start(TuiStartOptions{})</name> on the thread that owns the session.
                <name>Start</name> takes over the terminal and runs until <name>TUI::Stop()</name> is requested on that thread; it restores terminal state before returning or rethrowing a callback exception.
                Keep the listener alive until it is uninstalled, including when startup throws.
            </p>
            <p>
                <name>Starting</name>, <name>BufferSizeChanged</name>, keyboard and mouse callbacks, and <name>Timer</name> run on the owner thread.
                Use <name>TUI::StartTimer(milliseconds)</name> with a positive interval for periodic work.
                <name>Stop</name> requests exit; it does not immediately destroy the active session.
                <name>TUI::IsStopRequested()</name> distinguishes that request from <name>TUI::IsInUse()</name>, which remains true while the session is active.
                Active-session APIs require the owner thread.
            </p>
            <p>
                Run with interactive terminal input and output, without redirecting them to files or pipes.
                Ordinary <name>Console</name> operations are disabled while TUI owns the terminal.
                <name>TuiStartOptions::colorMode</name> accepts <name>Auto</name>, <name>TrueColor</name>, <name>Color256</name> or <name>Color16</name> from <name>TuiColorMode</name>.
                Query the selected mode with <name>TUI::GetColorMode()</name>.
                On POSIX, automatic detection uses <name>COLORTERM</name> for true color and <name>TERM</name> for 256 colors, falling back to 16 colors.
            </p>
        </topic>
        <topic>
            <title>Cell drawing and clipping</title>
            <p>
                The active buffer is available through <name>GetBuffer</name>, <name>GetBufferWidth</name> and <name>GetBufferHeight</name>.
                Reacquire its pointer and dimensions after <name>BufferSizeChanged</name>; resizing can replace the buffer.
                Draw with <name>PrintChar</name>, <name>DrawLineV</name>, <name>DrawLineH</name>, <name>DrawRect</name> and <name>Clear</name>, then call <name>RenderBuffer</name> to update the terminal.
                Each helper also has an overload taking <name>TuiPixel* buffer, vint width, vint height</name> for caller-owned buffers without an active terminal session.
            </p>
            <p>
                Every drawing overload accepts a final optional <name>const TuiClipper* clipper = nullptr</name>.
                The clipper covers <name>[x1, x2)</name> and <name>[y1, y2)</name> and is intersected with the buffer bounds; a null clipper uses the entire buffer.
                Its fields default to zero, so a pointer to a default-constructed clipper selects an empty area. Inverted bounds also paint nothing; clipping does not reorder them.
                Line, rectangle and <name>Clear</name> drawing coordinates include their endpoints.
                Clipping masks the original geometry, so it does not move a rectangle's border to the clip edge.
            </p>
            <p>
                For example, draw into a caller-owned 12 by 4 cell buffer while limiting writes to its inner area:
                <program language="C++"><code><![CDATA[
                    using namespace vl::console;

                    TuiPixel cells[12 * 4] = {};
                    TuiClipper clip{ 1, 1, 11, 3 };
                    TuiPrintOptions text;
                    TUI::Clear(cells, 12, 4, TuiColor{ 0, 0, 32 }, 0, 0, 11, 3, &clip);
                    TUI::PrintChar(cells, 12, 4, text, U'A', 1, 1, &clip);
                ]]></code></program>
            </p>
            <p>
                <name>MeasureChar</name> reports a scalar's cell width. A width-two character requires both cells inside the clip; a clipped half is not drawn.
                Drawing over either half of an existing wide character repairs its paired cell, even if that paired cell lies outside the clip, to keep the buffer valid.
                Prefer the drawing helpers over directly modifying wide-character and continuation cells.
            </p>
        </topic>
        <topic>
            <title>Portable input</title>
            <p>
                Keyboard, character and mouse events use the shared native input types from <name>vl::presentation</name>.
                Modifier fields, including <name>osSuper</name>, describe the events received from the terminal.
                The terminal or desktop can consume shortcuts before delivery, and POSIX terminal protocols do not always report physical key releases or distinguish every key combination.
                Check essential commands in the target terminal and provide alternatives for intercepted shortcuts.
            </p>
        </topic>
    </topic>
</article>

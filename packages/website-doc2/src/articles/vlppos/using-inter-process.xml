<article index="true">
    <topic>
        <title>Using Inter-Process Channels</title>
        <p>
            The <b>vl::inter_process</b> namespace is organized in layers.
            Feature code normally uses the channel interfaces, because they describe typed named message delivery between connected participants.
            Transport code implements the raw network protocol interfaces, because they describe how one server and many clients exchange asynchronous <b>WString</b> messages.
            The built-in Windows transports are composition choices that connect these two layers.
        </p>
        <p>
            Keep this boundary in application code:
            <ul>
                <li>Use <b>IChannelServer&lt;TPackage&gt;</b>, <b>IChannelClient&lt;TPackage&gt;</b>, <b>IChannel&lt;TPackage&gt;</b> and <b>IChannelReader&lt;TPackage&gt;</b> when the feature sends and receives packages.</li>
                <li>Use <b>INetworkProtocolServer</b>, <b>INetworkProtocolClient</b>, <b>INetworkProtocolConnection</b> and <b>INetworkProtocolCallback</b> when adding a new pipe, socket, HTTP, shared-memory or other underlying transport.</li>
                <li>Use <b>NetworkProtocolChannelServer&lt;TPackage, TSerialization, TServerBase&gt;</b> and <b>NetworkProtocolChannelClient&lt;TPackage, TSerialization&gt;</b> when a channel should run on top of a raw protocol implementation.</li>
            </ul>
        </p>
        <topic>
            <title>Channel Interfaces for Features</title>
            <p>
                A channel client is a participant with a server-assigned positive client id and a set of named channels.
                A channel is not a socket and it is not owned by the server alone.
                It is a logical lane shared by every connected participant that declares the same channel name.
            </p>
            <p>
                The channel interfaces are the stable API for feature code:
                <ul>
                    <li><b>IChannelReader&lt;TPackage&gt;::OnRead</b> receives the sender client id and one typed package.</li>
                    <li><b>IChannel&lt;TPackage&gt;</b> exposes <b>Initialize</b>, <b>SendToClient</b>, <b>BroadcastFromClient</b> and <b>BatchWrite</b>.</li>
                    <li><b>IChannelClient&lt;TPackage&gt;</b> exposes channel names, generated channel objects, client status, <b>WaitForServer</b> and lifecycle callbacks.</li>
                    <li><b>IChannelServer&lt;TPackage&gt;</b> accepts or rejects clients, records client ids, supports in-process local clients and broadcasts fatal channel errors.</li>
                </ul>
            </p>
            <p>
                Channel names are user-defined strings.
                A user channel name must not be empty and must not contain <b>!</b>.
                Names beginning with <b>!</b>, including <b>!Error</b> and <b>!System</b>, are reserved for the implementation.
            </p>
            <p>
                <b>Initialize</b> installs one reader and replays messages that arrived before the reader was installed.
                There is no reader uninstallation.
                <b>SendToClient</b> queues a direct message to one client id.
                <b>BroadcastFromClient</b> queues a message for all other participants on the same channel, optionally excluding selected receiver ids.
                <b>BatchWrite</b> flushes queued messages and reports disconnection through its output argument.
            </p>
            <p>
                <b>IChannelServer&lt;TPackage&gt;</b> manages delivery and bookkeeping.
                It is not itself a channel speaker.
                When server-side logic must send normal channel messages, create a <b>NetworkProtocolLocalChannelClient&lt;TPackage, TSerialization&gt;</b>, connect it with <b>ConnectLocalClient</b>, and send through that local client's channel objects.
                This keeps every normal channel message tied to a real positive sender client id.
            </p>
            <p>
                A small channel client follows this shape:
                <program language="C++"><code><![CDATA[
using namespace vl;
using namespace vl::collections;
using namespace vl::inter_process;

constexpr const wchar_t* ChatChannelName = L"Chat";

struct WStringListSerializer
{
    using SourceType = List<WString>;
    using DestType = WString;
    using ContextType = std::nullptr_t;

    static void Serialize(const ContextType&, const SourceType& source, DestType& dest)
    {
        dest = WString::Empty;
        for (auto&& item : source)
        {
            dest += itow(item.Length()) + L":" + item;
        }
    }

    static void Deserialize(const ContextType&, const DestType& dest, SourceType& source)
    {
        source.Clear();
        const wchar_t* reading = dest.Buffer();
        const wchar_t* end = reading + dest.Length();
        while (reading < end)
        {
            auto delimiter = wcschr(reading, L':');
            CHECK_ERROR(delimiter && delimiter < end, L"Invalid package.");

            auto length = wtoi(WString::CopyFrom(reading, (vint)(delimiter - reading)));
            CHECK_ERROR(length >= 0 && delimiter + 1 + length <= end, L"Invalid package length.");

            source.Add(WString::CopyFrom(delimiter + 1, length));
            reading = delimiter + 1 + length;
        }
    }
};

using ChatChannelClientBase =
    NetworkProtocolChannelClient<WString, WStringListSerializer>;

class ChatClient
    : public ChatChannelClientBase
    , public virtual IChannelReader<WString>
{
private:
    IChannelClient<WString>::ChannelMap channelNames;
    IChannel<WString>* chat = nullptr;

public:
    ChatClient(Ptr<INetworkProtocolClient> transport)
        : ChatChannelClientBase(transport)
    {
        channelNames.Add(ChatChannelName, nullptr);
        auto&& channels = GetChannels();
        auto index = channels.Keys().IndexOf(ChatChannelName);
        CHECK_ERROR(index != -1, L"Missing chat channel.");
        chat = channels.Values()[index];
        chat->Initialize(this);
    }

    const IChannelClient<WString>::ChannelNameList& OnGetChannelNames() override
    {
        return channelNames.Keys();
    }

    void OnConnected(vint clientId) override
    {
        // Record the assigned positive client id if the feature needs it.
    }

    void OnRead(vint senderClientId, const WString& package) override
    {
        // Handle one typed package from senderClientId.
    }

    void Send(vint receiverClientId, const WString& package)
    {
        bool disconnected = false;
        chat->SendToClient(receiverClientId, package);
        chat->BatchWrite(disconnected);
        if (disconnected)
        {
            // Stop using this client.
        }
    }
};
                ]]></code></program>
            </p>
        </topic>
        <topic>
            <title>Network Protocol Interfaces for Transports</title>
            <p>
                The raw protocol layer is the extension point for a new underlying implementation.
                It intentionally knows nothing about channel names or package types.
                One <b>INetworkProtocolConnection</b> exchanges asynchronous <b>WString</b> messages with the other side.
            </p>
            <p>
                Implement these interfaces for a new transport:
                <ul>
                    <li><b>INetworkProtocolConnection</b>: install or uninstall one callback, start the asynchronous read loop, send one string message and stop the connection.</li>
                    <li><b>INetworkProtocolCallback</b>: receive strings, remote errors, local errors, connected and disconnected events. Callback methods may run on any thread, so implementations and users must be thread-safe.</li>
                    <li><b>INetworkProtocolClient</b>: own one connection, block in <b>WaitForServer</b> until the connection is established or fails, and report <b>ClientStatus</b>.</li>
                    <li><b>INetworkProtocolServer</b>: start accepting, call <b>OnClientConnected</b> for accepted transport connections, stop all owned connections and report stopped state.</li>
                </ul>
            </p>
            <p>
                <b>InstallCallback</b> should call <b>OnInstalled</b> with the connection.
                Passing <b>nullptr</b> uninstalls the callback.
                <b>BeginReadingLoopUnsafe</b> starts receiving messages asynchronously, but callers must tolerate implementations that have already received data after the callback was installed.
                <b>Stop</b> is the shutdown boundary; after it returns, pending transport callbacks should no longer touch the stopped object.
            </p>
            <p>
                A raw client must return a connection object from <b>GetConnection</b>, but using that connection before <b>WaitForServer</b> finishes is outside the contract.
                A raw server should not report client-connected callbacks before <b>Start</b> or after <b>Stop</b>.
                If a connection is lost, the same connection object does not reconnect; a reconnecting transport should create a new connection object.
            </p>
            <p>
                After a raw transport exists, reuse the channel bridge instead of reimplementing channel routing:
                <program language="C++"><code><![CDATA[
class MyConnection : public Object, public virtual INetworkProtocolConnection
{
    // Store one callback, frame transport bytes into WString messages,
    // call OnReadString / OnLocalError / OnDisconnected, and drain callbacks in Stop.
};

class MyClient : public Object, public virtual INetworkProtocolClient
{
    // Own one MyConnection and implement WaitForServer / GetStatus.
};

class MyServer : public Object, public virtual INetworkProtocolServer
{
    // Accept MyConnection objects and pass them to OnClientConnected.
};

using MyChannelServer =
    NetworkProtocolChannelServer<MyPackage, MyPackageListSerializer, MyServer>;

using MyChannelClient =
    NetworkProtocolChannelClient<MyPackage, MyPackageListSerializer>;
                ]]></code></program>
            </p>
        </topic>
        <topic>
            <title>Default Channel Bridge</title>
            <p>
                <b>NetworkProtocolChannelServer</b>, <b>NetworkProtocolChannelClient</b> and <b>NetworkProtocolLocalChannelClient</b> translate channel batches into raw <b>WString</b> protocol messages.
                The bridge requires a serializer where:
                <ul>
                    <li><b>TSerialization::SourceType</b> is <b>collections::List&lt;TPackage&gt;</b>.</li>
                    <li><b>TSerialization::DestType</b> is <b>WString</b>.</li>
                    <li><b>TSerialization::ContextType</b> stores optional serializer state, or <b>std::nullptr_t</b> when no state is required.</li>
                    <li><b>Serialize</b> converts a package list to one <b>WString</b>.</li>
                    <li><b>Deserialize</b> converts one <b>WString</b> back to a package list.</li>
                </ul>
            </p>
            <p>
                The bridge uses <b>NetworkPackage</b> as the raw text envelope.
                Its string shape is <b>clientId,extraClientId1,...;channelName;messageBody</b>.
                For client-to-server messages, <b>clientId</b> is the direct receiver id; an empty id means broadcast, and extra ids are blocked broadcast receivers.
                For server-to-client messages, <b>clientId</b> is the positive sender id.
            </p>
            <p>
                A network channel client connects by sending one handshake package with an empty client id, an empty channel name and all supported channel names joined by <b>!</b>.
                The server validates the names, assigns a positive client id, records channel membership and sends the id back in an empty-channel response.
                After that, normal channel packages are delivered only when sender and receiver membership matches the channel name.
            </p>
            <p>
                A channel server over a Windows named pipe can be declared like this:
                <program language="C++"><code><![CDATA[
#include <vlppos.h>
#include <VlppOS.Windows.h>

using namespace vl;
using namespace vl::inter_process;

class ChatServer
    : public NetworkProtocolChannelServer<WString, WStringListSerializer, named_pipe::NamedPipeServer>
{
public:
    ChatServer(const WString& pipeName)
        : NetworkProtocolChannelServer<WString, WStringListSerializer, named_pipe::NamedPipeServer>(pipeName)
    {
    }

    WaitForClientResult OnClientConnected(
        vint clientId,
        const IChannelClient<WString>::ChannelNameList& availableChannels,
        IChannelClient<WString>* localClient) override
    {
        return availableChannels.Contains(ChatChannelName)
            ? WaitForClientResult::Accept
            : WaitForClientResult::Reject;
    }
};

auto server = Ptr(new ChatServer(L"ExamplePipe"));
server->Start();
                ]]></code></program>
            </p>
        </topic>
        <topic>
            <title>Windows Implementations</title>
            <p>
                The concrete raw protocol implementations currently provided by VlppOS are Windows-only.
                Treat them as ready-to-use Windows transports and as reference implementations for the raw protocol contract, but do not make portable feature code depend on them directly.
            </p>
            <topic>
                <title>Named Pipe Transport</title>
                <p>
                    <b>vl::inter_process::named_pipe::NamedPipeServer</b> implements <b>INetworkProtocolServer</b>.
                    <b>vl::inter_process::named_pipe::NamedPipeClient</b> derives from <b>vl::inter_process::named_pipe::NamedPipeConnection</b> and implements <b>INetworkProtocolClient</b>.
                    <b>vl::inter_process::named_pipe::NamedPipeConnection</b> implements <b>INetworkProtocolConnection</b>.
                </p>
                <p>
                    The server starts overlapped named-pipe accepting in <b>Start</b> and owns both accepted connections and pending accepts.
                    The client opens <b>\\.\pipe\NAME</b>, waits for the server and switches the pipe into message-read mode.
                    <b>SendString</b> frames one <b>WString</b> with length data.
                    The implementation chunks writes because a Windows named pipe message is limited to 64K bytes.
                    Broken-pipe cases become disconnection events, and fatal local pipe failures become <b>OnLocalError</b> followed by disconnection.
                </p>
            </topic>
            <topic>
                <title>HTTP Transport</title>
                <p>
                    <b>vl::inter_process::windows_http::HttpServer</b> derives from <b>vl::inter_process::windows_http::HttpServerApi</b> and implements <b>INetworkProtocolServer</b>.
                    <b>vl::inter_process::windows_http::HttpClient</b> implements both <b>INetworkProtocolClient</b> and <b>INetworkProtocolConnection</b>.
                    This transport is a raw protocol implementation over Windows HTTP APIs, not a general web framework.
                </p>
                <p>
                    The raw HTTP protocol uses these routes under the configured base URL:
                    <ul>
                        <li><b>GET /VlppInterProcess/Connect</b> creates a logical connection and returns per-connection request and response URLs.</li>
                        <li><b>POST /VlppInterProcess/Request/GUID</b> is the client-maintained long-poll request for server-to-client messages.</li>
                        <li><b>POST /VlppInterProcess/Response/GUID</b> sends client-to-server messages and may also receive one queued server-to-client message.</li>
                    </ul>
                </p>
                <p>
                    <b>vl::inter_process::windows_http::HttpClient::WaitForServer</b> sends the connect request, validates the returned URLs, records them and reports connection.
                    <b>BeginReadingLoopUnsafe</b> starts the long-poll request loop.
                    <b>SendString</b> posts to the response URL.
                    Connect and response failures retry a limited number of times; request failures retry while the client is still running.
                </p>
                <p>
                    <b>vl::inter_process::windows_http::HttpServer</b> creates a <b>vl::inter_process::windows_http::HttpServerConnection</b> for each connect request.
                    Server-to-client messages are returned through a pending long-poll request when possible, or queued until the next request.
                    Client-to-server request bodies are dispatched as inbound strings.
                    When the server stops, pending long-poll requests are cancelled and connection callbacks receive disconnection.
                </p>
            </topic>
            <topic>
                <title>Windows HTTP Helper APIs</title>
                <p>
                    <b>vl::inter_process::windows_http::HttpClientApi</b> and <b>vl::inter_process::windows_http::HttpServerApi</b> are lower-level Windows helpers used by <b>vl::inter_process::windows_http::HttpClient</b> and <b>vl::inter_process::windows_http::HttpServer</b>.
                    Use them directly when a Windows feature needs asynchronous HTTP request and response behavior without adopting the raw <b>INetworkProtocol</b> transport shape.
                </p>
                <p>
                    <b>vl::inter_process::windows_http::HttpClientApi</b> owns one WinHTTP session and connection for a host and port.
                    <b>HttpQuery</b> sends one asynchronous request described by <b>vl::inter_process::windows_http::HttpRequest</b>, whose fields include method, query, body, content type, accept types, credentials, cookies, extra headers and timeouts.
                    Results are either <b>vl::inter_process::windows_http::HttpResponse</b> or <b>vl::inter_process::windows_http::HttpError</b>; HTTP status codes such as 404 are represented as <b>HttpResponse</b> values.
                </p>
                <p>
                    <b>vl::inter_process::windows_http::HttpServerApi</b> owns one HTTP.sys URL prefix.
                    Override <b>OnHttpRequestReceived</b> to dispatch requests and <b>OnHttpServerStopping</b> to clean up feature state.
                    Helpers such as <b>GetUtf8Body</b>, <b>SendResponse</b> and <b>SendResponseUtf8</b> handle common request-body and response tasks.
                </p>
            </topic>
        </topic>
    </topic>
</article>

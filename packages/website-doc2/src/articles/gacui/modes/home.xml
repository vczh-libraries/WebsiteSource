<article index="true">
    <topic>
        <title>Hosted Mode and Remote Protocol</title>
        <p>
            Using <b>SetupHostedWindowsGDIRenderer</b> or <b>SetupHostedWindowsDirect2DRenderer</b> instead of <b>SetupWindowsGDIRenderer</b> or <b>SetupWindowsDirect2DRenderer</b>
            runs a GacUI application in hosted mode.
            A hosted mode GacUI application will start only one OS native window,
            other windows are rendered inside it virtually.
            System dialogs will be replaced by predefined GacUI implemented dialogs by default,
            so that anything will be strictly inside the OS native window.
        </p>
        <p>
            Using <b>SetupRemoteNativeController</b> runs a GacUI application with remote protocol (forced in hosted mode),
            which becomes headless,
            instead of render anything on the screen,
            it sends out rendering commands to a remote client.
            This part will be covered in <a href="/gacui/modes/remote_core.html">Remote Protocol Core Application</a>.
        </p>
        <p>
            If you want a remote protocol renderer that still work with other components of GacUI,
            <b>GuiRemoteRendererSingle</b> implements everything that the remote protocol needs and starts a window,
            displaying anything from the core GacUI application,
            with user interactions covered.
            This part will be covered in <a href="/gacui/modes/remote_client.html">Remote Protocol Client Application</a>.
        </p>
        <p>
            The <a href="https://github.com/vczh-libraries/GacJS">GacJS</a> also implements a GacUI renderer in a browser,
            connecting to a GacUI application via HTTP protocol.
            But the HTTP protocol implementation is not official,
            it is for demo only,
            no security guarantee is provided.
        </p>
        <p>
            GacUI provides a JSON channel layer for the remote protocol,
            implemented with <b>vl::inter_process</b>.
            A C++ core application usually starts a <b>GuiRemoteProtocolNetworkChannelServer</b>,
            connects itself through <b>GuiRemoteProtocolLocalChannelClient</b>,
            and waits for a renderer connected through <b>GuiRemoteProtocolChannelClient</b>.
            The underlying data-transmission implementation is still selected by the application.
            Current Windows examples use:
            <ul>
                <li><b>vl::inter_process::named_pipe::NamedPipeServer</b> and <b>vl::inter_process::named_pipe::NamedPipeClient</b>.</li>
                <li><b>vl::inter_process::windows_http::HttpServer</b> and <b>vl::inter_process::windows_http::HttpClient</b>.</li>
            </ul>
        </p>
        <p>
            A different data-transmission implementation should be added by implementing the <b>vl::inter_process</b>
            <b>INetworkProtocolConnection</b>, <b>INetworkProtocolCallback</b>, <b>INetworkProtocolClient</b> and <b>INetworkProtocolServer</b> interfaces.
            They are named network protocol interfaces, but the implementation does not have to be a network protocol;
            stdio, DLL function calls, shared memory, sockets or any other message transport can fit this layer.
            so the GacUI JSON channel classes can stay unchanged.
            This part will be covered in <a href="/gacui/modes/remote_communication.html">Remote Protocol Channel Layer</a>.
        </p>
        <p>
            The complete Windows test projects are
            <a href="https://github.com/vczh-libraries/GacUI/tree/master/Test/GacUISrc/RemotingTest_Core">RemotingTest_Core</a> and
            <a href="https://github.com/vczh-libraries/GacUI/tree/master/Test/GacUISrc/RemotingTest_Rendering_Win32">RemotingTest_Rendering_Win32</a>.
            They show the current named pipe and HTTP setup.
        </p>
    </topic>
</article>

<article index="true">
    <topic>
        <title>Remote Protocol Channel Layer</title>
        <p>
            GacUI remote protocol channeling uses the vl::inter_process channel bridge and INetworkProtocolXXX transport interfaces underneath.
            The GacUI layer remains a rendering protocol between a headless core and a renderer.
        </p>
        <p>
            The layers are:
            <ul>
                <li>IGuiRemoteProtocol and IGuiRemoteEventProcessor: the strongly typed GacUI remote protocol.</li>
                <li>GuiRemoteProtocolCoreChannel and GuiRemoteProtocolRendererChannel: conversion between the strongly typed protocol and JSON channel packages.</li>
                <li>GuiRemoteProtocolNetworkChannelServer, GuiRemoteProtocolLocalChannelClient and GuiRemoteProtocolChannelClient: the vl::inter_process JSON channel bridge.</li>
                <li>NamedPipeServer, NamedPipeClient, HttpServer, HttpClient, or a custom INetworkProtocolXXX implementation: the underlying data-transmission implementation.</li>
            </ul>
        </p>
        <p>
            The JSON channel package type is Ptr&lt;glr::json::JsonNode&gt;.
            The remote protocol channel name is GacUIRemoteProtocolChannelName, whose value is GacUIRemoteProtocol.
            The in-process core client should be assigned GacUIRemoteProtocolCoreClientId.
            Renderer-side packages are sent to that core client id, and core-side packages are sent to the renderer client id learned from ControllerConnect.
        </p>
        <p>
            ChannelPackageInfo describes the remote protocol envelope:
            <ul>
                <li>semantic: Message, Request, Response or Event.</li>
                <li>id: the request or response id, or -1 when the package does not need one.</li>
                <li>name: the generated remote protocol message, request, response or event name.</li>
            </ul>
        </p>
        <p>
            JsonChannelPack and JsonChannelUnpack convert this envelope and the optional arguments node to and from a JSON object.
            Application code normally does not call these functions unless it is logging, debugging or building a non-C++ renderer.
        </p>
        <topic>
            <title>What User Code Owns</title>
            <p>
                A normal C++ application configures the remote protocol channel at startup.
                After GuiRemoteProtocolCoreChannel and GuiRemoteProtocolRendererChannel are connected,
                the library routes individual remote protocol messages between the strongly typed protocol and JSON channel packages.
            </p>
            <p>
                User code usually owns only:
                <ul>
                    <li>A small server policy around GuiRemoteProtocolNetworkChannelServer, if the core needs to wait for a renderer, reject invalid clients, or replace a previous renderer.</li>
                    <li>An underlying data-transmission implementation, represented by INetworkProtocolServer, INetworkProtocolClient and INetworkProtocolConnection.</li>
                    <li>Optional renderer-side error handling by overriding GuiRemoteProtocolChannelClient::OnReadError, OnLocalError or OnDisconnected.</li>
                </ul>
            </p>
            <p>
                GuiRemoteProtocolCoreChannel exposes BeforeWrite and BeforeOnRead.
                GuiRemoteProtocolRendererChannel exposes the same events.
                Use these events for tracing package flow or diagnosing mismatched renderer/core behavior.
            </p>
        </topic>
        <topic>
            <title>Adding a Transport</title>
            <p>
                Add a new underlying data-transmission implementation below GacUI by implementing the vl::inter_process INetworkProtocolXXX interfaces.
                The names say network protocol, but the contract is only asynchronous WString message exchange.
                The implementation can be a pipe, socket, stdio stream, shared memory, DLL function calls, or any other mechanism that can satisfy INetworkProtocolConnection, INetworkProtocolClient and INetworkProtocolServer.
                Then reuse the GacUI JSON channel classes.
            </p>
            <p>
                The core side selects the INetworkProtocolServer implementation as the template argument:
                <program language="c++"><code><![CDATA[
using MyRemoteCoreServer =
    GuiRemoteProtocolNetworkChannelServer<MyNetworkProtocolServer>;

auto jsonParser = Ptr(new glr::json::Parser);
MyRemoteCoreServer server(jsonParser, /* transport server constructor arguments */);
server.Start();

auto coreClient = Ptr(new GuiRemoteProtocolLocalChannelClient(jsonParser));
server.ConnectLocalClient(coreClient);
                ]]></code></program>
            </p>
            <p>
                The renderer side wraps an INetworkProtocolClient object:
                <program language="c++"><code><![CDATA[
auto jsonParser = Ptr(new glr::json::Parser);
auto networkClient = Ptr(new MyNetworkProtocolClient(/* connection arguments */));

GuiRemoteProtocolChannelClient channelClient(networkClient, jsonParser);
channelClient.WaitForServer();
                ]]></code></program>
            </p>
            <p>
                The same channel setup shown in
                <a href="/gacui/modes/remote_core.html">Remote Protocol Core Application</a> and
                <a href="/gacui/modes/remote_client.html">Remote Protocol Client Application</a>
                works after the transport object is connected.
                See <a href="/vlppos/using-inter-process.html">Using Inter-Process Channels</a>
                for the INetworkProtocolXXX and channel interface contracts.
            </p>
        </topic>
    </topic>
</article>

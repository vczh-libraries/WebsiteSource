<article index="true">
    <topic>
        <title>&lt;ez:Layout&gt;</title>
        <p>
            <b>&lt;ez:Layout/&gt;</b> is a bounds composition that arranges controls using docking,
            proportional space, and grids. The <b>ez</b> namespace is predefined in GacUI XML resources.
            Its child layout descriptions are not controls or compositions.
        </p>
        <topic>
            <title>Arranging Content</title>
            <p>
                Use <b>&lt;ez:Top/&gt;</b> and <b>&lt;ez:Bottom/&gt;</b> for vertical docking,
                or <b>&lt;ez:Left/&gt;</b> and <b>&lt;ez:Right/&gt;</b> for horizontal docking.
                <b>&lt;ez:Fill/&gt;</b> shares the remaining space using its <b>Percentage</b> weight, which defaults to 1.
                For a group containing only fills, <b>Direction="Horizontal"</b> or <b>Direction="Vertical"</b>
                selects the arrangement; otherwise it inherits the enclosing arrangement, defaulting to horizontal at the root.
            </p>
            <p>
                For grids, put <b>&lt;ez:Column/&gt;</b> descriptions inside <b>&lt;ez:Row/&gt;</b>, or rows inside columns.
                <b>CellOption</b> selects minimum, absolute, or proportional sizing, and <b>CellSpan</b> spans tracks.
                Place an empty <b>&lt;ez:Splitter/&gt;</b> between tracks to let users resize adjacent absolute-sized tracks.
            </p>
            <p>
                Each layout or description can contain one control or independently positioned composition,
                or a group of child descriptions.
                Do not mix horizontal and vertical docking at the same level, or fills with rows or columns.
                Nest groups to combine these arrangements.
                For example, a vertical layout can contain a horizontal button group:
                <program language="XML"><code><![CDATA[
                    <ez:Layout>
                      <ez:Fill><MultilineTextBox/></ez:Fill>
                      <ez:Bottom>
                        <ez:Right><Button Text="OK"/></ez:Right>
                        <ez:Right><Button Text="Cancel"/></ez:Right>
                      </ez:Bottom>
                    </ez:Layout>
                ]]></code></program>
            </p>
            <p>
                Nesting <b>&lt;ez:Layout/&gt;</b> is supported but strongly discouraged for combining arrangements.
                Use nested description groups within one owning layout instead;
                different groups can have different arrangements without an extra layout owner.
            </p>
        </topic>
        <topic>
            <title>Spacing</title>
            <p>
                <b>Padding</b> sets the gap between neighboring parts and defaults to 5.
                Nested description groups do not multiply that gap.
                <b>Border</b> defaults to <b>true</b> and adds the same gap around the layout;
                set it to <b>false</b> to remove that outside gap. It does not draw a border.
            </p>
        </topic>
        <topic>
            <title>Updating the Layout</title>
            <p>
                XML resources build the layout automatically, and normal resizing needs no extra work.
                Call <b>BuildLayout()</b> after changing layout descriptions or their properties.
                Rebuilding keeps existing controls, their state, bindings, and event handlers.
            </p>
            <p>
                <b>Padding</b>, <b>Border</b>, <b>Direction</b>, and <b>CellSpan</b> support bindings;
                later changes take effect when <b>BuildLayout()</b> is called.
                <b>Percentage</b> and <b>CellOption</b> must be constants in XML.
            </p>
        </topic>
    </topic>
</article>
